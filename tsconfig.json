{
  "extends": "astro/tsconfigs/strictest",
  "compilerOptions": {
    // Let me pass `undefined` explicitly to option properties.
    "exactOptionalPropertyTypes": false,

    "baseUrl": ".",
    "paths": {
      "~*": ["src/*"]
    }
  }
}
